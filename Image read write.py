import cv2

#-------------------------------- Image Read,write ----------------------------------------------------

# imp = cv2.imread('lena.jpg',-1)
# print(imp)

# cv2.imshow('image',imp)
#cv2.waitKey(5000) # 5000 means we need show image in 5 seconds
# k = cv2.waitKey(0) & 0xFF # if the waitkey is 0 the popup window for the image will never close automatically

# if k == 27:
    # cv2.destroyAllWindows() # to destroy window after shown the image
# elif k == ord("s"):
    # cv2.imwrite('lena_copy.png',imp)
    # cv2.destroyAllWindows() # to destroy window after shown the image

#------------------------------------- End -----------------------------------------------------------  






